name: release released

on:
  release:
    types:
      - released

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check
        env:
          EVENT: ${{ github.event }}
        run: echo $EVENT
